# Copiar a `.env.local` si corres Next en el host (`npm run web` desde soldmate-frontend).
# Con el stack Docker del repo, el backend publica el API en el puerto 28080 del host.
NEXT_PUBLIC_API_URL=http://localhost:28080
